---
security_fail2ban_base_jail_configuration:
  - option: bantime
    value: "{{ security_fail2ban_bantime }}"
    section: DEFAULT
  - option: maxretry
    value: "{{ security_fail2ban_maxretry }}"
    section: DEFAULT
